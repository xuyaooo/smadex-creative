[pytest]
markers =
    slow: integration tests requiring full pipeline
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::FutureWarning
